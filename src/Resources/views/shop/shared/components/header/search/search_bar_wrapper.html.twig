{# Real searchbar renderer — hidden until SearchBarMoverController moves it into the desktop/mobile anchor #}
<div
    data-controller="gally--sylius-plugin--search-bar-mover"
    aria-hidden="true"
    style="display:none"
>
    {# The actual search bar — rendered once, no sub-request #}
    <div
        data-gally--sylius-plugin--search-bar-mover-target="bar"
        data-controller="gally--sylius-plugin--search-autocomplete"
        data-gally--sylius-plugin--search-autocomplete-preview-url-value="{{ path('gally_search_result_preview_ajax') }}"
        data-action="mousedown@document->gally--sylius-plugin--search-autocomplete#outsideClick"
        class="searchFormContainer"
    >
        {{ form_start(searchForm) }}
        <div class="input-group mb-2 mt-2 mt-lg-0 mb-lg-1 px-2 px-lg-0">
            {{ form_widget(searchForm.query, {'attr': {
                'data-gally--sylius-plugin--search-autocomplete-target': 'input',
                'data-action': 'input->gally--sylius-plugin--search-autocomplete#onInput focus->gally--sylius-plugin--search-autocomplete#onFocus',
            }}) }}
            <button type="submit" class="btn btn-outline-secondary btn-sm collapsed" data-bs-toggle="collapse" data-bs-target="#gally-search-results" aria-expanded="false" aria-controls="gally-search-results">
                {{ ux_icon('tabler:search') }}
            </button>
        </div>
        {{ form_end(searchForm) }}
        <div class="position-absolute d-flex justify-content-center" style="z-index: 10000">
            <div id="gally-search-results" class="card collapsedSearchResults collapse mx-2 mx-lg-0 p-3" style="max-width: 930px" data-gally--sylius-plugin--search-autocomplete-target="resultsPanel">
                <div class="loading-results text-center d-none" data-gally--sylius-plugin--search-autocomplete-target="loading">
                    <div class="spinner-border" role="status">
                        <span class="visually-hidden">Loading...</span>
                    </div>
                </div>
                <div class="results" data-gally--sylius-plugin--search-autocomplete-target="results">
                </div>
            </div>
        </div>
    </div>
</div>
